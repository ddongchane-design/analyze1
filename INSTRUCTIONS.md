# AI 에이전트 기반 분석 가이드 (INSTRUCTIONS.md)

이 가이드는 로컬 컴퓨터(회사 노트북, 개인 데스크톱 등)에 **Gemini API Key가 설정되어 있지 않거나 환경이 다른 상황**에서도, IDE 대화창의 **AI 에이전트(Antigravity 등)와 병렬 서브에이전트(Subagents)**를 활용해 유튜브 자막 분석 및 대시보드 갱신 작업을 동일하게 수행할 수 있도록 안내하는 지침서입니다.

---

## 1. 개요 및 동작 메커니즘
이 프로젝트는 로컬에서 API를 호출하는 대신, **AI 에이전트의 자체 추론 능력과 병렬 서브에이전트 기능**을 활용해 수십 개의 비디오를 분석합니다. 
1. 사용자가 `data/pending/`에 분석할 영상 자막 JSON 파일을 넣어둡니다.
2. 대화창을 통해 에이전트에게 **"pending에 있는 영상들을 병렬 서브에이전트로 분석하고 대시보드를 업데이트해줘"**라고 지시합니다.
3. 에이전트가 `data/pending/` 목록을 읽고, **4~5개 그룹(Batch)으로 분할**하여 병렬 서브에이전트를 생성합니다.
4. 서브에이전트들은 개별 자막을 읽어 분석 및 분류를 수행하고 결과를 `data/analyzed/{topic_id}/{video_id}.json`에 저장한 뒤, 기존 pending 파일을 삭제합니다.
5. 에이전트가 최종 종합 인사이트(synthesis) 캐시를 갱신하고 `python main.py --render` 명령어를 실행하여 대시보드를 빌드합니다.
6. 마지막으로 `git`을 통해 결과를 원격 저장소에 푸시합니다.

---

## 2. 작업 순서 (Step-by-step Workflow)

### Step 1. 로컬 환경 동기화
새로운 작업을 시작하기 전에 깃허브 최신 코드를 당겨옵니다.
```bash
git pull
```

### Step 2. 대화창에 분석 요청하기
대화창에 아래와 같이 요청합니다:
> **요청 예시:**
> "data/pending에 있는 미분석 영상 리스트를 읽어줘. 그리고 서브에이전트들을 병렬로 기동해서 분석을 수행해주고, 분석이 끝나면 누락된 토픽의 종합 인사이트(synthesis)를 갱신한 뒤 대시보드 렌더링(python main.py --render)까지 완료해줘."

### Step 3. 에이전트의 서브에이전트 기동 (자동)
에이전트는 내부적으로 `invoke_subagent` 툴을 사용해 작업을 분할합니다. (각 서브에이전트에게 내릴 구체적인 프롬프트 지침은 **4. 에이전트 분석용 프롬프트 템플릿** 참고)

### Step 4. 터미널 명령 승인하기
작업 도중 에이전트가 로컬에서 명령어를 실행하려고 할 때(예: `python main.py --render`, `git add`, `git commit` 등), 화면에 **승인(Approve)** 버튼이 팝업됩니다.
* ⚠️ **중요**: 대화창에서 명령 실행 대기 상태로 멈춰 있을 때, 반드시 우측 하단이나 팝업창에서 **'승인(Approve)'** 버튼을 클릭해주어야 작업이 중단 없이 진행됩니다.

### Step 5. 깃허브 푸시 (자동 및 승인)
분석 완료 후 에이전트가 변경된 분석 파일(`data/analyzed`), 종합 인사이트(`data/synthesis`), 렌더링된 HTML(`output/html/`)을 커밋하고 푸시합니다.

---

## 3. 핵심 주의사항
1. **`data/synthesis/economy.json` 보호**: 
   * 사용자가 매크로 경제 등 특정 분야의 종합 인사이트(`economy.json`)를 수동으로 수정한 경우, AI가 이를 자동 재생성하여 **덮어쓰지 않도록** 프롬프트상에서 강력히 제안해야 합니다.
2. **동작 속도 최적화**: 
   * 분석할 영상이 20개 이상일 경우, 한 번에 처리하면 컨텍스트 제약 및 시간 초과가 발생하므로 **서브에이전트 4~5개에 나누어 병렬 처리**하도록 해야 합니다.
3. **하이라이트 클래스 적용**: 
   * 분석 결과 텍스트 내 주요 용어에는 대시보드 스타일이 입혀질 수 있도록 반드시 아래의 HTML 태그가 적용되어야 합니다.

---

## 4. 에이전트 분석용 프롬프트 템플릿
에이전트가 서브에이전트를 생성할 때 사용하는 프롬프트 규격입니다.

### [A] 영상 분석 프롬프트
```markdown
당신은 테크/투자 전문 분석가입니다.
아래 유튜브 영상 자막을 분석해서 반드시 JSON 형식으로만 답하세요.

영상 제목: {title}
채널: {channel}
자막: {자막 앞부분 8000자 내외}

응답 형식 (JSON만, 다른 텍스트 없이):
{
  "summary": "3줄 핵심 요약",
  "key_claims": ["핵심 주장 1", "핵심 주장 2", "핵심 주장 3"],
  "data_points": ["언급된 수치/데이터 1", "수치 2"],
  "signal": "bullish", // bullish, bearish, neutral 중 하나
  "signal_reason": "시그널 판단 근거 1~2줄",
  "key_companies": ["언급 기업/종목 1", "종목 2"],
  "insight": "단순 요약이 아닌 핵심 인사이트 (왜 중요한지, 어떤 의미인지)",
  "action_point": "이 영상을 보고 투자자가 주목해야 할 행동 포인트"
}
```

### [B] HTML 하이라이팅 적용 규칙
분석 결과의 `summary`, `insight`, `action_point`, `signal_reason`, `key_claims` 항목 내 핵심 단어들에 아래 태그를 감싸줍니다. (단락당 2~3개 제한)
* **메가트렌드, 거시 흐름, 근본 원인**: `<span class="text-amber-300 font-bold">강조단어</span>`
* **수혜주, 구체적 부품/기술명, 핵심 기업명**: `<span class="text-cyan-300 font-semibold">강조단어</span>`
* **시장 경고, 리스크, 투자 위험성, 음의 시그널**: `<span class="text-rose-400 font-medium">강조단어</span>`
* **미중 갈등, 지정학적 이슈, 중국 공급망**: `<span class="text-violet-300 font-medium">강조단어</span>`

### [C] 주제 분류 규칙
분석 결과를 바탕으로 아래 8개 토픽 중 가장 적합한 대주제 ID 하나(`primary_topic`)를 매칭합니다.
* `robot`: 로봇/피지컬AI
* `economy`: 경제/매크로
* `tech`: 테크/AI
* `stock`: 주식/투자
* `energy`: 에너지
* `crypto`: 크립토/암호화폐
* `space`: 우주산업
* `etc`: 기타/시사/뉴스

---

이 지침서가 루트 디렉토리에 존재하면, 회사 노트북이나 집 데스크톱 등 **어느 환경의 AI 에이전트라도 지침서를 먼저 읽고 로컬 API Key 오류 없이 대화창을 통해 분석 및 렌더링 작업을 성공적으로 인계**받을 수 있습니다.
